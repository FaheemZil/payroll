import 'package:flutter/material.dart';

class AppColors {
  static const primaryColor = Color.fromRGBO(0, 136, 169, 1);
  static const secondaryColor = Color(0xFF304c76);

  static const Color textTertiary = Color(0xFF75D6FF);


   /// Alert yello
  ///  ```
  /// Color(0xFFFFFFFF)
  /// ```
  static const Color alertYellow = Color(0xFFF3C523);

  /// Secondary Appbar Background
  ///  ```
  /// Color(0xFFFFFFFF)
  /// ```
  static const Color appBarPrimary = Color(0xFFFFFFFF);

  /// Primary Appbar Background
  ///  ```
  /// Color(0xFF3C4687)
  /// ```
  static const Color appBarSecondary = Color(0xFF3C4687);

  /// Primary Scaffold Color
  ///  ```
  /// Color(0xffFFFFFF)
  /// ```
  static const Color backgroundPrimary = Color(0xffFFFFFF);

  /// Secondary Scaffold Color
  ///  ```
  /// Color(0xff3C4687)
  /// ```
  static const Color backgroundSecondary = Color(0xff3C4687);

  /// White Color
  ///  ```
  /// Color(0xFFFFFFFF)
  /// ```
  static const Color black = Color(0xFF000000);

  /// Primary blue used in backgrounds, appbar, buttons
  ///  ```
  /// Color(0xff4F5AA3)
  /// ```
  static const Color blueDark = Color(0xff4F5AA3);

  /// Primary blue used in backgrounds, appbar, buttons
  ///  ```
  /// Color(0xff2E9BFF)
  /// ```
  static const Color blueLight = Color(0xff478DF6);

  static const borderColor = Color.fromRGBO(215, 222, 236, 1);

  /// Alternate blue color used in switch buttons
  ///  ```
  /// Color(0xFF576CF2)
  /// ```
  static const Color blueLightAlt = Color(0xFF576CF2);

  ///  Blue color used in text buttons
  ///  ```
  /// Color(0xFF2C78EA)
  /// ```
  static const Color blueTextButtonColor = Color(0xFF2C78EA);

  /// Creammy
  ///  ```
  /// Color(0xFFF5F5F5)
  /// ```
  static const Color creammy = Color(0xFFF5F5F5);

  /// Divider color
  ///  ```
  /// Color(0xFFD1D1D1)
  /// ```
  static const Color divider = Color(0xFFE6E6E6);

  /// Background color for the children of an expansion tile
  ///  ```
  /// Color(0xFFFFFFFF)
  /// ```
  static const Color expansionTileContent = Color(0xFFF0F6FF);





  static const Color inputBorderDisabled = Color(0xFF6F6F6F);
  static const Color inactiveBg = Color(0xFFFAFAFA);

  /// Input border for white background
  ///  ```
  /// Color(0xFFDBDBDB)
  /// ```
  static const Color inputBorderPrimary = Color(0xFFDBDBDB);

  /// Input border for color background
  ///  ```
  /// Color(0xFFA8CBFF)
  /// ```
  static const Color inputBorderSecondary = Color(0xFFA8CBFF);

  /// Error Color
  ///  ```
  /// Color(0xFFF26B6B)
  /// ```
  static const Color inputError = Color(0xFFF26B6B);



  /// Blue like logo color
  ///  ```
  /// Color(0xFF415A80)
  /// ```
  static const Color logoColor = Color(0xFF415A80);

  /// Grey like color
  ///  ```
  /// Color(0xFFABABAB)
  /// ```
  static const Color menuIconCOlor = Color(0xFFABABAB);

  /// Material Popup Menu border color
  ///  ```
  /// Color(0xFFD1D1D1)
  /// ```
  static const Color popupMenuBorder = Color(0xFFD1D1D1);

  /// Black text color.
  ///  ```
  /// Color(0xFFFFFFFF)
  /// ```
  static const Color textBlack = Color(0xFF060606);

  /// Light color used in color buttons etc.
  ///  ```
  /// Color(0xFFFFFFFF)
  /// ```
  static const Color textLight = Color(0xFFFFFFFF);

  /// Normal text black color.
  ///  ```
  /// Color(0xFF282B29)
  /// ```
  static const Color textNormalBlack = Color(0xFF282B29);
  static const Color greyCard = Color.fromRGBO(235, 235, 235, 1);

  /// Primary color used in input field text, app text etc.
  /// ```
  /// Color(0xFF3F3F3F)
  /// ```
  static const Color textPrimary = Color(0xFF3F3F3F);

  /// Secondary color used in labels, disabled text etc.
  /// ```
  /// Color(0xFF6F6F6F)
  /// ```
  static const Color textSecondary = Color(0xFF6F6F6F);

  /// Tertiary text color used in dark background
  ///  ```
  /// Color(0xFF75D6FF)
  /// ```


  /// Alternate tertiary text color used in light background
  ///  ```
  /// Color(0xFF576CF2)
  /// ```
  static const Color textTertiaryAlt = Color(0xFF576CF2);

  /// White Color
  ///  ```
  /// Color(0xFFFFFFFF)
  /// ```
  static const Color white = Color(0xFFFFFFFF);
  static const Color inActiveThumb = Color(0xffE2E2E2);

  /// button Shadow Color
  ///  ```
  /// Color(0x3F5291DA)
  /// ```
  static const Color buttonShadow = Color(0x3F5291DA);

  /// Shadow Color
  ///  ```
  /// Color(0xff3662B9)
  /// ```
  static const Color shadowColor = Color(0xff3662B9);

  /// Orange color
  ///  ```
  /// Color(0xFFfa9c12)
  /// ```
  static const Color orange = Color(0xFFfa9c12);
  static const Color warning = Color(0xFFF0950C);

  /// Secondary Yellow color
  ///  ```
  /// Color(0xFFFFFCF4)
  /// ```
  static const Color secondaryYellow = Color(0xFFFFFCF4);

  /// Secondary Red color
  ///  ```
  /// Color(0xFFFFFCF4)
  /// ```
  static const Color secondaryRed = Color(0xFFFFF3F3);

  /// Secondary Green color
  ///  ```
  /// Color(0xFFFFFCF4)
  /// ```

  /// Color used in disabling, disabled buttons etc.
  /// ```
  /// Color(0xFFE0E0E0)
  /// ```
  static const Color buttonDisable = Color(0xFFE0E0E0);

  /// Color used in disabled texts, disabled buttons texts etc.
  /// ```
  /// Color(0xFFAAAAAA)
  /// ```
  static const Color disabledText = Color(0xFFAAAAAA);

  static const Color secondaryGreen = Color(0xFFF2FFF8);
  static const Color cloudBankOne = Color(0xFFCC3AFF);
  static const Color cloudBankTwo = Color(0xFF63ABFF);
  static const Color cloudBankThree = Color(0xFFFFC34E);
  static const Color bgBankOne = Color(0xFFFEF4FF);
  static const Color bgBankTwo = Color(0xFFF2F9FF);
  static const Color bgBankThree = Color(0xFFFFFDF5);
}
